spring.application.name=demo
spring.data.mongodb.uri=mongodb://localhost:27017/shelterconnect

# Increase max file size for base64 photo uploads if they are large
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.port=8080
